//Пишем функцию, которая это (1,2,3,4) делает по нажатию!//
function numbers() {
    var numbers = [1, 2, 3, 4];
    for (var number of numbers) {  // перебор элементов
        console.log(number);
    }
}

